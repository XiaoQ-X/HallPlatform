# 霍尔效应平台公式核对报告

核对范围：网页副效应模型、四方向采样与服务端入库、Unity 核心模型、数据拟合、不确定度传播、题库与课题说明。

## 已验证公式

- 经典霍尔电压：`V_H = R_H I B / d`；`R_H = V_H d / (I B)`。
- 单载流子浓度：`n = 1 / (e |R_H|)`。
- 四方向修正：槽位为 `V1(+I,+B)、V2(+I,-B)、V3(-I,-B)、V4(-I,+B)`，因此 `Vcorr = (V1 - V2 + V3 - V4) / 4 = V_H + V_E`。不等位电压 `V0 ∝ I`、能斯特项 `V_N ∝ B`、里纪–勒杜克项 `V_RL ∝ B` 和固定零点项被抵消；厄廷豪森项 `V_E ∝ I B` 与霍尔项同对称性，不能仅靠换向消除。
- 副效应模型：四种横向磁效应为 `V⊥ = V_H + V_E + V_N + V_RL`，实际测量为 `Vmeasured = V⊥ + V0 + Voffset`；`V_0 = r_0 I`、`V_E = k_E I B`、`V_N = k_N B`、`V_RL = k_RL B`。页面输入 `kE` 的 `mV/(mA·T)` 与底层 `V/(A·T)` 数值等价，`kN/kRL` 的 `mV/T` 转换为 `V/T` 时除以 1000。
- 不确定度：重复测量 `u_A=s/√n`；均匀、正态、三角分布分别使用 `Δ/√3、Δ/3、Δ/√6`；霍尔系数和载流子浓度使用独立输入的一阶传播。
- Unity 导电支路：`R=ρL/(wt)`、`ρ=1/(n e μ)`；线圈端电压：`U=RI+L·dI/dt`；磁场模型为 `B=μ₀(H+M)`，其中 `M` 采用明确标注为未标定的 Langevin 教学曲线。
- 量子霍尔表述：第 `ν` 个平台的霍尔电阻为 `R_xy=h/(νe²)=R_K/ν`，冯·克利青常数为 `R_K=h/e²`；电磁流量计资料区分 `E=B D v` 中的磁场 `B` 与流速 `v`，扫描仿真磁场不等于改变流速。

## 已修正问题

1. 温度梯度路径此前对 `S·∇T·L_E` 无条件取绝对值，已改为保留热电系数和温度梯度的符号。
2. 服务端副效应记录此前只检查有限数值，已改为按参数重新计算四方向读数、修正值、残余 `V_E` 和理想 `V_H`，不一致的数据会被拒绝。
3. 服务端四方向采样现在校验槽位与方向映射、四个方向的电流/磁场幅值及电压接线方向一致，再执行合成。
4. Unity 数据页与平台侧栏将合成量标为 `Vcorr=V_H+V_E`，明确它未扣除厄廷豪森附加电压；VH-IM 说明已改为“未饱和区近似线性，接近饱和会偏离”。
5. 课题与案例说明已与当前磁芯饱和模型和 `IM≤1 A` 引擎边界对齐；量子霍尔内容使用 `R_xy=h/(νe²)` 表示霍尔电阻平台。
6. 种子题库和案例任务中的量子霍尔、运动电动势表述已纠正，避免把 `R_K` 与平台电阻混写或把磁场扫描误写成流速扫描。
7. 独立副效应案例页的状态改为“完整组 N · 本组方向 M/4”，重复执行四方向测量会提示先清空重测，不会重复计组或重复提交同一组数据。
8. Unity 工作台的浏览器全屏退出层、测试仪焦点控制、旋钮量程/当前示数和接线撤销均已加入实时验收；WebGL 事件中的 `V0/VE/VN/VRL/noise` 分项可供平台复核。

## 验证结果与限制

共享计算、服务端接口和端到端副效应流程回归测试通过；当前本地回归为 89/89，生产前端构建成功，浏览器 smoke 覆盖 20 个师生路由且无 pageerror/横向溢出，完整业务浏览器工作流 17 项通过，Unity WebGL 构建标记为 `HALL_WEBGL_BUILD_PASS 12683241`。实时页面实测四方向读数得到 `Vcorr=-1.070 mV`、`VE=0.010 mV`、`VH=-1.080 mV`，与上述模型一致。Unity 的 `n、μ、线圈参数、磁路耦合、厄廷豪森/错位/噪声系数` 是教学演示参数，不是实物仪器标定值；平台已在界面和日志中标注这一限制。`VH-IM` 的线性拟合只能作为未饱和区的近似或经验拟合，不能据此宣称全量程严格线性。
