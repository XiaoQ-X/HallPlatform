# 霍尔效应平台：第二轮业务体验与修复

日期：2026-09-19。项目：`D:\开发项目\最终平台\HallPlatform`。
运行入口：http://127.0.0.1:8182 。数据库迁移版本：144。

## 结论与范围

本轮继续修复第一轮遗漏的业务问题，覆盖教师、学生、同伴评阅三类操作。下表列出本轮发现和处理的 40 项问题，其中相邻条目可能属于同一根因。它们不是对软件中所有未知缺陷的穷尽证明。

自动化测试报告为 31 项通过、0 失败（其中包含两个父测试）；浏览器验收 17 条跨角色流程通过，未捕获页面脚本异常。另对 19 个教师/学生页面进行了加载与桌面横向溢出检查；Unity 单独运行验证，仍有明确阻塞。页面加载检查不等于页面每一个按钮、每一种输入组合都验证通过。

测试使用隔离数据库和合成验收账号，未向实际教学数据库灌入验收提交、分数或申诉。原 ZIP 和原始备份未修改。

## 本轮修复清单

验证说明：API = 自动化接口/数据库回归；浏览器 = 实际浏览器操作与断言；代码 = 静态检查及构建，尚无该项独立交互回归。

| 编号 | 问题与业务影响 | 处理结果 | 验证 |
|---|---|---|---|
| 01 | 单选答案可填不存在的选项，多选可重复字符 | 校验实际选项范围、数量、单选长度、多选去重 | API |
| 02 | 判断题和填空候选答案可保存无效格式 | 判断题限对/错，候选 JSON 必须为非空字符串数组 | API（判断题）、代码（填空） |
| 03 | 归档已发布试卷引用的题目会破坏试卷 | 引用检查，先调整或下架试卷才允许归档 | API |
| 04 | 教师撤下试卷后，已开始答卷无法提交 | 以本人答卷快照判卷，不再依赖当前发布状态 | API |
| 05 | 撤下后刷新页面无法继续既有答卷 | 增加本人答卷恢复接口及历史中的继续入口，不返回标准答案 | API、浏览器 |
| 06 | 超时后仍有 30 秒可更改答案 | 严格使用服务端截止时间，超时仅按此前草稿判分 | API |
| 07 | 超时判草稿，但结果页展示客户端最后答案 | 接口返回实际判分答案，结果页据此展示 | 代码、相关 API |
| 08 | 简答题已评后不能纠分，页面隐藏已评答卷 | 按快照识别简答题，支持复核、反馈、通知与审计 | API、浏览器 |
| 09 | 试卷编辑显示“新建”，保存强制发布 | 编辑/新建标题区分；新卷默认草稿；显式发布勾选 | 页面检查、构建 |
| 10 | 案例编辑无法设置量程，保存可能覆盖原配置 | 增加电流量程和横轴控制，保留其余既有配置 | 代码 |
| 11 | 资源编辑取消后嵌套附件等仍可能被修改 | 编辑表单深拷贝，避免共享对象 | 代码 |
| 12 | 任务进度接受字符串 false 等伪布尔值 | 必须提交与任务数匹配的布尔数组 | API |
| 13 | 修改任务模板后旧勾选错配到新任务 | 保存模板哈希，模板变化后旧勾选不自动沿用 | 代码、迁移 |
| 14 | 提交报告未固定当时任务完成状态 | 提交保存任务进度快照；页面提交前保存进度 | API |
| 15 | 已评分报告界面仍可编辑上传，最后才报错 | 编辑、上传、删除附件和重交入口显示锁定状态 | 浏览器 |
| 16 | 退回后理由消失，重交可能混用旧反馈 | 保留退回理由，成功重交清理上一轮反馈 | API、浏览器 |
| 17 | 历史报告已存库但师生无法查看 | 新增有权限检查的版本接口及师生历史内容/评分/附件入口 | API、浏览器（学生） |
| 18 | 教师看着旧报告可误评学生刚提交的新版本 | 新界面提交版本号，不一致返回 409 并要求重开报告 | API；兼容旧 API 调用时版本号仍可省略 |
| 19 | 互评引用活动报告，重交改变既有评分证据 | 发布时冻结报告版本、内容、附件、任务，实验作品冻结测量数据 | API |
| 20 | 同一成果版本重复发布产生多份作品 | 同来源、版本发布返回既有作品，并显示提示 | API |
| 21 | 普通文件可冒充录屏 | 要求本人上传的视频类型，沿用文件内容校验 | API |
| 22 | 转班后仍能读取旧班分配作品 | 清理失效分配，列表同时校验当前班级 | API |
| 23 | 未评分前能查看别人的评分，影响独立互评 | 尚未提交评价的评阅人不返回其他评价 | 代码 |
| 24 | 已撤销的同伴评分仍显示为普通有效分数 | 明示“已撤销（不计分）”，汇总继续排除 | 代码、既有撤销流程回归 |
| 25 | 申诉成功后残留引用，下一条反馈误关联旧成绩 | 成功后清空预置申诉对象 | 代码 |
| 26 | 分析历史载入对 Vue 代理 structuredClone 报错 | 对 JSON 数据深拷贝并恢复轴、来源、清洗版本 | 浏览器 |
| 27 | 量表复制同样触发 structuredClone 报错 | 深拷贝维度并保留分档说明 | 浏览器 |
| 28 | 导入空实验会话仍残留上一会话的数据点 | 切换时清空旧数据及来源版本 | 代码 |
| 29 | 快速切换实验会话，慢响应覆盖最新选择 | 响应写回前检查仍为当前会话 | 代码 |
| 30 | 清洗保存/删除后有效点状态不刷新 | 同时刷新台账和当前会话巡检数据 | 代码 |
| 31 | 编辑较早清洗记录仍被较新记录 ID 覆盖 | 按修改时间和 ID 确定当前决策，毫秒级记录修改时间 | API |
| 32 | 极端数值计算溢出，JSON 保存后变成 null | 计算中间矩阵及结果递归有限值检查，明确拒绝溢出 | 数值回归 |
| 33 | 附件组件复用后缓存旧文件下载地址 | 源地址变化时取消请求并释放旧 Blob URL | 代码 |
| 34 | 无实验学生的完成数为 null，页面空白 | SQL COALESCE 返回 0 | 代码、页面加载 |
| 35 | 看板“自测最佳”实际为逐卷最佳再平均 | 标签改为“自测综合”以匹配现有政策 | 代码 |
| 36 | 推送重复点击、类型切换请求乱序、草稿可选 | 增加发送状态；检查资源类型；仅列已发布资源 | 浏览器（正常推送）、代码（竞态） |
| 37 | 同一路由切换通知资源 ID 不更新详情 | 案例、思政、课题监听查询参数变化 | 代码；浏览器验证通知到案例 |
| 38 | 答题历史仅展示前 5 条，无更多入口 | 展示全部答题记录，可恢复旧的在答记录 | 浏览器、代码；大数据分页仍待优化 |
| 39 | 视频未传完就能发布作品，上传可能写回别的表单 | 上传期间禁止发布，发布忙状态与表单对象检查 | 代码 |
| 40 | 新建学生提供无效的“未分班”选择 | 明确必须选择任课班级，密码框使用密码类型 | 页面检查、代码 |

## 实际体验证据

教师端页面检查：数据看板、资源管理、学生管理、推送、教学评阅、量表、成绩和申诉。学生端页面检查：首页、案例、思政、自测、课题、清洗、不确定度、分析、作品互评、申诉和成绩。另加载运行 Unity。

17 条浏览器流程的逐条结果见 `全功能复测/跨角色流程结果.json`，包括：报告合法零分；简答题初评和纠分；学生发布报告；被分配同伴评阅；教师撤销申诉评价；教师发布成绩与学生读取；拟合残差和置信区间；390px 移动端；富文本清洗；教师实际下载附件；量表复制发布；分析历史恢复；不确定度历史恢复；撤卷恢复提交；推送到通知深链接；报告锁定、退回、第二版重交及历史保留。

量表复制修复后截图：

![量表复制后保留维度并生成新版本](全功能复测/量表复制验收.png)

截图采用标准浏览器验收输出。部分嵌入浏览器截图有裁切/拼接异常，没有把其外观异常认定为已确认的产品缺陷；DOM 几何检查显示侧栏右边界为 256px、正文标题左边界为 280px，未重叠。

## 仍未解决或未充分验收

不能把本次结果表述为“所有问题已解决”。第一轮报告里的“部分解决”状态仍有效，除本报告明确补齐的子项外，没有自动升级为完成。

| 问题或能力缺口 | 现状与可执行方案 |
|---|---|
| Unity 无法稳定记录，缺源工程 | 本轮再次观察到回路正常、IS=3mA、IM约0.4A，等待约27秒仍返回 READING_UNSTABLE；完整组为0。需要与本包对应的 Unity C# 工程、ProjectSettings/Packages 和桥接代码，检查噪声、判稳阈值、更新时间及物理标定后重建，再完成四方向采集。不能伪造事件绕过。 |
| 仿真刷新后不能完整恢复接线/半组状态 | 前端待确认队列已有持久化，但 Unity 缺完整状态导入协议；需源工程增加可校验的状态导出/导入。 |
| 学科内容和真实仪器资料 | 厂家误差限、型号手册、思政引语出处及细化的量表行为须由课程资料支持；应按来源逐条审校，不能填入未经核实的数据。 |
| 课程政策仍固定 | 必做/选做、各项权重、考试次数、课题及互评截止时间、多课程/学期/多人授课尚无完整配置模型。方案：课程和任务发布版本表，成绩快照固定政策版本，补迁移与跨学期回归。 |
| 推送管理能力 | 支持发送和通知读取；尚无撤回与教师逐人阅读回执。方案：记录推送-通知关系、读取时间及撤回状态。 |
| 数据处理扩展 | 目前支持独立量一阶传播与指定k；协方差、有效自由度及自动k尚未实现。完整PDF实验报告未实现。应在明确统计模型后扩展并用参考数据验证。 |
| 大文件、容量与故障恢复 | 视频使用鉴权整文件Blob；孤立上传自动清理、断点续传尚未实现。大班并发、磁盘满、断电、长时断网及异机离线部署未压测。需实际目标硬件和网络条件开展容量与故障注入测试。 |
| 查询规模和完整可访问性 | 部分N+1、整表读取和历史列表仍存在；全站键盘焦点陷阱、屏幕阅读器及所有手机断点未专项验收。应按真实数据量分页/索引优化，按对话框组件统一键盘行为。 |
| 正式部署 | 本机仅监听127.0.0.1，不是公网或全班共享部署。HTTPS、反向代理、运维账户和异机恢复需要在目标部署环境验收。 |

## 更新与回退记录

更新前数据库与上传备份：`D:\开发项目\最终平台\HallPlatform\backups\2026-09-19T10-43-19-693Z`。
数据库迁移144增加互评证据/来源版本、任务模板哈希。启动后 `PRAGMA integrity_check` 返回 `ok`；入口HTTP返回200。

已停止本轮隔离验收服务8183；本机平台8182继续运行。实际教师凭据未再次重置，沿用已交付的《本机教师登录凭据.txt》。迁移前备份不包含密钥，恢复需遵循原维护说明；不能用迁移前数据库直接搭配新代码假定无需重启迁移。

复跑：`node --test tests/*.test.cjs`、`node tests/browser-workflow.cjs`、`node tests/browser-smoke.cjs`、`pnpm build`。浏览器脚本当前使用本机 Edge 与本机 Playwright 路径，异机需调整。smoke脚本中的Unity失败要阅读结果字段，不能只看进程退出码。

本报告补充而不覆盖第一轮72项问题清单；两份报告共同构成本次项目的已知问题、处理措施和未完成范围。
