# 霍尔效应教学平台 v1.4 整改与验收报告

日期：2026-09-19。项目目录：`D:\开发项目\最终平台\HallPlatform`。

> **历史基线说明（2026-09-22）**：本报告记录 2026-09-19 尚未取得 Unity C# 源工程时的整改结论，其中第 71 项“受阻”和“Unity 没有源工程”的描述仅适用于当日基线。当前交付已补齐核心工作台与微观机理演示的可重建 Unity 源工程及对应 WebGL 产物；最新构建、物理口径和浏览器验收以仓库根目录 `README-使用与部署说明.md` 以及两个 Unity 工程 README 为准。本报告其余历史测试数据和未覆盖范围仍然保留，不能替代当前发布验收。

已在用户指定目录解压、实施修复、迁移原数据库、构建并启动。本机地址为 http://127.0.0.1:8182 。没有覆盖原始压缩包；另有 `D:\开发项目\最终平台\原始备份`。

**本次完成 Web 平台主要故障与已验证安全漏洞的修复，但不能宣称72项全部关闭。** Unity没有源工程，真实测量判稳仍阻塞；学校评分政策、完整物理审校及部署容量验收也不能由接口测试替代。下表明确区分完成与剩余工作。

## 已验证结果

| 验证 | 结果与范围 |
|---|---|
| `node --test tests/*.test.cjs` | 15项通过，0失败。包括数值边界、置信区间、霍尔传播、认证、越权、B卷、事件重放/回滚、清洗、课题、互评、上传、旧令牌、过期答案、旧作品迁移、成绩发布。 |
| `node node_modules/vite/bin/vite.js build` | 生产构建成功。 |
| `pnpm install --frozen-lockfile` | 锁定安装通过；使用pnpm 11.19.0。 |
| `pnpm audit --json` | 本次检查返回0条已知漏洞。ECharts已从5.6升级至6.1；这不等于证明依赖没有未知问题。 |
| `node tests/browser-workflow.cjs` | 10项跨角色浏览器检查通过，0个pageerror，详见下文。 |
| `node tests/browser-smoke.cjs` | 11条教师/学生页面路由打开，无pageerror；真实Unity加载、接线、参数命令与事件入库可运行。完整四方向实验未通过。 |
| `node scripts/backup.cjs work/final-backup` | SQLite一致快照+附件+SHA-256清单生成成功。 |
| `node scripts/restore.cjs work/final-backup work/restore-verified` | 在全新目录校验并恢复成功。用户7、实验事件19、附件5、成绩快照0，源与恢复数量一致；没有覆盖原数据。 |
| 实际本机实例 | 随机密码教师登录200，看板200，五份课题附件经授权下载200。 |

跨角色浏览器验收包含：教师报告评零分、简答阅卷、学生发布课题作品、分配同伴完成量表评分、申诉撤销裁决、成绩发布与学生查看、拟合/残差/95%区间渲染、390px移动端无横向溢出、富文本剔除事件属性和危险协议且保留公式、教师实际下载学生报告附件。使用隔离测试数据库和合成作业输入，不冒充学生真实实验。

## 72项逐项状态

编号对应原《项目审查与整改方案》。这里“已修复”表示原问题的主要可定位故障已修复，并非所有可能扩展需求都已实现。“部分解决”列明剩余边界。

| 编号 | 状态 | 本次处理与剩余工作 |
|---|---|---|
| 01 | 已修复 | 移除共享硬编码密钥；随机本机密钥/生产环境密钥；JWT限定HS256并查询真实用户、状态和token_version。伪造旧密钥/不存在用户回归被拒。 |
| 02 | 已修复 | 不自动初始化演示账号；旧公开密码账号锁定；页面删除公开凭据；管理员工具生成随机密码。当前teacher已重置，其余旧学生仍锁定。 |
| 03 | 已修复 | 所有会话写入先校验所有者；跨学生请求403且无派生写入。 |
| 04 | 部分解决 | 原始四方向、数值范围、状态、完整组校验及服务端合成；实验计分必须教师核验。浏览器来源不能证明真实操作，物理/任务完成要求仍需Unity和教学验收。 |
| 05 | 已修复 | 类型白名单、文件头检测、随机名称、强制下载、nosniff和附件CSP；HTML/伪PDF拒绝。文档病毒扫描尚未接入。 |
| 06 | 部分解决 | 文件所有者/教师班级/分享作品权限；旧公开目录关闭；1GB个人配额和上传并发限制。孤立文件自动回收未实现，视频仍整文件取Blob，超大录屏需专项测试。 |
| 07 | 已修复 | 用户返回字段白名单，教师列表无密码哈希。 |
| 08 | 已修复 | 关闭公开注册，由教师管理名单；班级归属和学号冲突校验；数据库唯一约束在无历史冲突时建立。 |
| 09 | 已修复 | 改密、教师重置、退出撤销旧令牌，访问令牌8小时；个人改密返回新令牌。 |
| 10 | 已修复 | DOMPurify清洗富文本；浏览器验证危险属性和URL剔除，KaTeX公式保留。 |
| 11 | 部分解决 | 统一基础校验、事务、JSON错误及登录限流；修复空格密码一致性。并非所有接口都已改为完整声明式schema，大规模恶意流量需网关限流。 |
| 12 | 部分解决 | 班级teacher_id和资源owner_id隔离；推送、案例初始化、量表及题目按教师归属检查。完整多课程/学期/多人授课关系尚未实现。 |
| 13 | 已修复 | 迁移历史非数值分值，重新计算旧答卷并建快照；种子函数兼容原调用；数据库分值约束；B卷正确答卷不再产生null或字符串总分。部分旧解析原本缺失，保留诚实的通用提示。 |
| 14 | 已修复 | 题库仅教师可读；学生开始答卷不含答案；过期未提交答卷也不暴露snapshot/detail；只公布已提交答卷内容。 |
| 15 | 已修复 | 候选答案结构化处理，不按绝对值符号拆分，标准式1/(e\|R_H\|)回归判对。不是任意数学表达式等价判定器。 |
| 16 | 已修复 | 题目可发布/编辑，作答和解析使用答卷快照。 |
| 17 | 已修复 | 课题ref访问修正，真实浏览器新建保存通过。 |
| 18 | 已修复 | 新增教师教学评阅页，查看报告/附件、评分反馈、退回重交；0分评阅和下载真实验收通过。 |
| 19 | 已修复 | 简答逐题人工评分、待评状态和总分重算；已提交未评课题/答卷阻止正式发布。 |
| 20 | 已修复 | 服务端截止时间、自动草稿、返回保存、恢复答题，停止离开后的页面计时器。 |
| 21 | 部分解决 | attempt生命周期、服务端限时、重复提交幂等；仍采用可重复自测，尚未分离正式考试次数政策。 |
| 22 | 已修复 | 空卷、无效题、非法分值/时长、重复题和跨教师题目引用被拒。 |
| 23 | 部分解决 | 答卷、报告、评价有快照，删除资源改归档；历史不会被直接删除。还不是完整发布版本管理系统，修改后新作答采用新内容。 |
| 24 | 已修复 | 课题存在、发布、课程权限和附件归属验证，非法课题提交不落库。 |
| 25 | 部分解决 | 已评分报告锁定，教师退回才可重交；保留版本和审计。尚无课题截止时间管理，历史版本的教师比较界面未实现。 |
| 26 | 已修复 | 用null区别未评与合法0分，反馈可见。 |
| 27 | 部分解决 | 支持真实附件上传/下载；五个课题安装真实任务单并验收可下载。没有伪造厂家手册，具体型号数据手册与仪器误差限仍需课程方提供。 |
| 28 | 已修复 | 学生任务勾选在服务端独立保存并回填；不是实验证据自动核验。 |
| 29 | 已修复 | 案例参数与任务元数据编辑受引擎能力约束；试卷编辑、发布/归档可操作。 |
| 30 | 部分解决 | 所有案例改为合法固定硅参数；不支持的开关/电机/量子任务明确为阅读或类比。引擎能力未扩展，真实完整测量尚受判稳问题阻塞。 |
| 31 | 已修复 | 事件与派生数据统一幂等，event_id唯一；重复请求不重复生成测量。 |
| 32 | 已修复 | 批次事务、明确ACK；坏事件回滚，响应丢失可重放。 |
| 33 | 部分解决 | 按用户持久化待确认队列，失败重试，入库ACK后才删除。浏览器清除存储/配额耗尽等情况下仍需用户暂停并处理，未做断电与所有浏览器专项演练。 |
| 34 | 已修复 | 重启前确认保存并放弃旧会话，建立新会话后接收新事件，不再依赖固定延时清空缓存。 |
| 35 | 部分解决 | 恢复未确认队列并保留旧记录，提供放弃状态。缺Unity完整状态导入，刷新后不能恢复接线/当前半组并继续原场景。 |
| 36 | 部分解决 | 校验消息origin/source；服务端案例配置为准；Init成功才可操作；显示桥接错误。全命令成功/超时协议仍受现有Unity接口限制。 |
| 37 | 已修复 | 初始方向0/4；进度条改为本组方向进度，不再把6组硬编码当整个实验百分比。 |
| 38 | 部分解决 | 历史演示会话标记demo，实验统计/计分排除，演示事件不进入正式记录。历史演示原始数值不当作真实数据修造，仍保留供辨识。 |
| 39 | 已修复 | 清洗按点保留/排除，分析默认使用清洗后数据，保存清洗版本和证据快照。 |
| 40 | 已修复 | 增加完整组、重复组、有限数值检测；同条件至少5组用MAD提示可疑值，保留人工判因而非自动删点。未声称检测所有物理异常。 |
| 41 | 已修复 | 清洗记录可编辑、默认未解决、关联测量点、审计修改。 |
| 42 | 已修复 | 严格数字校验；空白不能参与计算；[1,2]均值1.5。 |
| 43 | 已修复 | 至少两次重复测量，误差限非负，包含因子正值，无效值不给结果。 |
| 44 | 部分解决 | 可导入同条件清洗数据；独立SI输入量传播RH与n、标准/扩展不确定度。协方差、有效自由度自动确定k、相关量完整模型仍未实现。 |
| 45 | 已修复 | QR/SVD中心化拟合，重复自变量/共线拒绝，恒定响应R²显示未定义，避免NaN保存。 |
| 46 | 已修复 | 轴切换重新映射导入数据，模型和图表同步。 |
| 47 | 已修复 | 单变量拟合按固定条件/系列选择；多变量模型显式选择，默认排除已清洗点。物理系列意义仍需实验教师核验。 |
| 48 | 已修复 | 新增y=a+bx+cz多自变量拟合、预测值及残差可视化。 |
| 49 | 部分解决 | 保存数据哈希、来源点/清洗快照、算法版本、自由度、标准误和95%区间；历史载入与JSON导出。未提供自动生成的完整PDF实验报告。 |
| 50 | 已修复 | 发布后的ref赋值修正，关闭表单并刷新，浏览器作品发布通过。 |
| 51 | 已修复 | 维度完整、有限数值、上下界、量表类型/归属/绑定验证；历史非法分值不参与正常汇总。 |
| 52 | 已修复 | 新量表版本管理与作品绑定，评价留维度快照；旧作品迁移保留历史量表和评阅人。 |
| 53 | 部分解决 | 滑块与存储统一初始化0；补充分档描述，教师可创建新量表。默认描述较通用，学科分档行为仍需任课教师细化。 |
| 54 | 部分解决 | 均衡分配最多2名同班评阅人，支持补齐、完成统计和会话/成果关联。截止时间、录屏内录制、提交版本不可变引用还不完整。 |
| 55 | 部分解决 | 申诉证据快照、维持/撤销、审计与通知；撤销影响草稿，教师重新发布形成新成绩版本。成绩申诉改分仍需在成绩页调整后发布，不是单步原子裁决。 |
| 56 | 已修复 | 实验分改为教师核验分，不再按所有历史异常自动扣分。 |
| 57 | 部分解决 | 改为逐卷最高已评成绩再平均，未完成计0并提示。仍采用固定“所有已发布试卷”政策，必做/选做与正式考试规则未配置化。 |
| 58 | 部分解决 | 所有已发布课题计入平均，漏交不被平均分排除；未评已交成果阻止发布。学校必做/选做权重仍需配置扩展。 |
| 59 | 已修复 | 互评质量70%+分配任务完成30%，分配数作分母，合法得分不超过100。 |
| 60 | 部分解决 | 调分理由、操作者和前后值审计；发布不可变快照含权重和政策。权重仍固定，尚无可视化课程权重配置。 |
| 61 | 已修复 | 学生查看本人发布成绩、分项、政策和待完成项，可发起成绩申诉。 |
| 62 | 部分解决 | 看板按教师班级隔离，完成率按学生去重，连续7天趋势、未核验/未解决指标。完整数据质量分布及大班查询优化尚未完成。 |
| 63 | 部分解决 | 资源深链接、推送目标切换清理、资源所有者校验；试卷链接定位相应卡片。推送撤销和逐人阅读回执未实现。 |
| 64 | 已修复 | CSV转义逗号/引号/换行，公式样式文本加前缀避免被当公式。 |
| 65 | 已修复 | 通知分页、独立全量未读计数、离开清理轮询。 |
| 66 | 部分解决 | 迁移加入外键表、关系保护触发器、数值/唯一约束和索引；历史孤儿/重复项审计。没有破坏性重建全部旧表，历史冲突需要教师/运维核对。 |
| 67 | 部分解决 | 增加核心关系索引、异步登录密码比较、上传配额。仍有N+1和整表取数、部分同步计算；未做实际整班并发/大文件/磁盘满压测。 |
| 68 | 已修复 | 全局错误提示、相同进行中请求去重、请求超时、教师路由角色守卫。 |
| 69 | 已修复 | 新增可运行API/数值/浏览器回归，pnpm锁定安装与依赖审计；图表库已升级并验证。浏览器测试路径目前与本机运行环境绑定。 |
| 70 | 部分解决 | 更新安全部署、HTTPS/上传限制、离线前置条件；提供备份与不覆盖式恢复，完成本机恢复演练。异机/跨OS离线部署、学校代理与断电恢复仍待验收。 |
| 71 | 受阻 | 原包只有Unity WebGL产物，没有C#源工程及可重建项目。可留存现有产物哈希，但无法补造源工程或修复引擎内部逻辑。 |
| 72 | 部分解决 | 纠正流量计题、量子霍尔电阻符号、纵向测量任务及案例能力表述；任务单列参考出处。全部思政事实/引语来源及物理精度尚须课程专家逐条审校。 |

## 独立安全复核

按安全修复流程完成一次独立只读调查与一次独立候选复核。候选复核确认三处额外问题，均已修复并加入回归：

1. 过期答卷经历史接口暴露答案：改为仅明确已提交状态可披露，未提交/过期状态剔除快照和解析。
2. 合法首尾空格密码不能登录：密码校验保留原始字符串，设置与登录语义一致。
3. 旧作品失去互评入口：迁移143补齐量表和分配并保留既有评阅人；有冲突时留审计，不任意改旧评分依据。

已验证安全修复结果为 **fixed**，范围包括伪造旧JWT、无用户令牌、跨学生会话写入、提前答案读取、危险上传、无权限附件读取、非法互评分数与旧令牌继续使用。原触发在回归中被拒；教师正常登录、学生答题、合法上传下载、作品评价和评分在API与浏览器验证中保持可用。此结论不覆盖所有未知攻击面或公网容量保障。

主要涉及 `server/routes/{auth,sim,resources,upload,files,peer,teacher,workshop}.js`、`server/{config,migrate,content-migration,integrity-migration,legacy-review-migration,grades}.js`、`shared/calculations.cjs` 及对应Vue页面。新增 `tests/platform.test.cjs`、`tests/calculations.test.cjs`、`tests/browser-smoke.cjs`、`tests/browser-workflow.cjs`。当前已构建dist，未提交或推送远程仓库。

## Unity阻塞证据与后续验收

本机Edge无头环境中：六根线连接后 `powered=true`、`circuitValid=true`、`IS_mA=3`、`IM_A≈0.4`。等待约27秒调用RecordMeasurement，引擎仍返回 `READING_UNSTABLE`，没有生成完整组。不能由此断言所有课堂机器都失败，也不能宣称全流程实验已经完成。

解除阻塞需要与现有WebGL版本对应的Unity源工程，或在目标课堂机器前台操作的判稳/完整四方向证据。取得工程后应定位电流/磁场稳定阈值、噪声、时间更新与桥接接口；重新验证VH合成符号、B-IM标定、固定条件和归一化。不能通过网页伪造“稳定”或测量事件来宣布修复。

## 交付与维护

- 启动脚本：项目目录的 `启动平台.ps1`；默认8182，已运行时不要重复启动。
- 教师账号：`teacher`。随机初始密码见单独的《本机教师登录凭据.txt》；登录后改密并删除凭据文件。旧学生需由教师重置，不继续使用公开密码。
- 运行日志：`work/server.stdout.log`、`work/server.stderr.log`。Node进程由本次启动，非开机自启系统服务。
- 完整维护说明：项目 `README-使用与部署说明.md`，交付副本为《平台启动与维护说明.md》。
- 发布前最低剩余门槛：真实Unity四方向贯通、教师确认课程评分政策与内容、目标班级并发/大录屏/断网恢复和HTTPS部署演练。

附件：浏览器验收结果、移动端截图、分析图表截图和Unity阻塞证据；截图仅证明所示状态，不替代完整物理验收。
